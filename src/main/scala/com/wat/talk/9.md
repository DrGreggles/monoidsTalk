# Laws

### Monoids have *laws*

- **Associativity**
    - `(a + b) + c == a + (b + c)`
    - "No need for brackets"
- **Identity**
    - `0 + a == a`
    - `a + 0 == a`

### Who cares?

- Makes it behave "as expected"
- Associativity important for e.g. parallelism with fs2